# Commit Message

## Korean

```
docs(github-access): GitHub 접근 권한 문서 페이지 삭제

인앱 UI(조직 탭 설치 유도, 에러 시 인라인 도움말)가 이미 사용자를 충분히 안내하고 있어
별도 문서 페이지의 실용적 가치가 낮다고 판단.

페이지, 컴포넌트, i18n 메시지(en/ko), 사이드바 항목, E2E 테스트를 모두 제거.
```

## English

```
docs(github-access): remove GitHub access documentation page

In-app UI (org tab install prompts, inline error guidance) already provides sufficient
user guidance, making the dedicated docs page redundant.

Removed page, component, i18n messages (en/ko), sidebar entry, and E2E tests.
```
